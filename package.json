{
  "name": "point-click-claude",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "validate": "node tools/validate.ts",
    "fuzz": "node tools/fuzz.ts",
    "test": "node --test tools/core.test.ts",
    "check": "npm run typecheck && npm test && npm run validate && npm run fuzz"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
